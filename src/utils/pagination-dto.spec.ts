import { Utils\paginationDto } from './utils\pagination-dto';

describe('Utils\paginationDto', () => {
  it('should be defined', () => {
    expect(new Utils\paginationDto()).toBeDefined();
  });
});
