import { Utils\userExistRule } from './utils\user-exist-rule';

describe('Utils\userExistRule', () => {
  it('should be defined', () => {
    expect(new Utils\userExistRule()).toBeDefined();
  });
});
